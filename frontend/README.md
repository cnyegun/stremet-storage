# Stremet frontend

## Setup

1. Install dependencies:

```bash
npm install
```

2. Make sure the backend API is running on `http://localhost:4000`.

3. Start the frontend:

```bash
npm run dev
```

The app uses `NEXT_PUBLIC_API_BASE_URL` from `.env.local` and expects the shared types in `../shared/types.ts`.
